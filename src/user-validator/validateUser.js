const axios = require('axios')
async function canPostEvent(id) {
    // if (!id) {
    //     return false
    // }
    // const isValid = await axios.post('http://umsapi.com/check-user', {
    //     role: postEvent
    // })

    // if (isValid.data.isValid) {
    //    return true
    // }
    return true
}

module.exports = canPostEvent